server.port=8080

# Data source
# Local
spring.datasource.url=jdbc:postgresql://gti710-20183-06.logti.etsmtl.ca:5432/bas_de_bon_sens
spring.datasource.username=dba_gti710
spring.datasource.password=Gti710Pass06


# JPA Properties
spring.jpa.generate-ddl=false
spring.jpa.show-sql=false
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL82Dialect
spring.jpa.hibernate.use-new-id-generator-mappings=true

# Disable Spring Security
security.ignored=/**

# Specify the Lucene Directory
spring.jpa.properties.hibernate.search.default.directory_provider = filesystem

# Using the filesystem DirectoryProvider you also have to specify the default
# base directory for all indexes
spring.jpa.properties.hibernate.search.default.indexBase = indexpath